# Generated by @doctorine/sdk-codegen (Doctorine SDK Foundry). DO NOT EDIT.
[build-system]
requires = ["hatchling>=1.25,<2"]
build-backend = "hatchling.build"

[project]
name = "parcel-events-sdk-fixture-260904"
version = "1.2.0"
description = "Python SDK generated by the Doctorine SDK Foundry (sdkSha bf47bc76880f)."
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.28.1,<0.29",
  "pydantic>=2.11.7,<3",
]

# The W2.2/W4.2 verify pins as ranges — a local `uv run --group dev` sees
# the factory toolchain (one source of truth: PY_VERIFY_TOOLCHAIN).
[dependency-groups]
dev = [
  "mypy>=1.15.0,<2",
  "pytest>=8.3.5,<9",
  "ruff>=0.12.4,<0.13",
]

[tool.hatch.build.targets.wheel]
packages = ["parcel_events_sdk_fixture_260904"]

# The SHIPPED lint config — the factory `python.lint` verify step runs
# exactly this, so `ruff check .` locally IS the gate. The vendored
# kernel is excluded: it carries its own (stricter) upstream gate.
[tool.ruff]
target-version = "py310"
extend-exclude = ["**/_kernel"]

# The SHIPPED strict-typing config — the factory `python.typecheck` step.
[tool.mypy]
strict = true
python_version = "3.10"
warn_unreachable = true

# The SHIPPED smoke config — the factory `python.smoke` step.
[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
